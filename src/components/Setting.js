import SettingIcon from "../assets/images/setting.svg";

const Setting = () => {
  return (
    <img
      src={SettingIcon}
      alt='setting'
      width={40}
      height={40}
      className='cursor-pointer'
    />
  );
};

export default Setting;
